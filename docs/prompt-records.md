# Prompt 记录

> **模块：** `成员代码/xiezhizhuo/Client.py` 安全整改  
> **记录人：** renyanbin  
> **工具：** Claude (claude.ai 网页版)

---

## Prompt 记录 #1 — 风险识别辅助

**使用场景：** 动手改代码前，用 AI 辅助梳理安全风险

**完整 Prompt：**

```
你是一位信息安全工程师。请审查以下 Python TCP 文件下载客户端代码，
识别其中存在的安全风险，并按严重程度（高/中/低）排序，
每个风险给出：漏洞类型、攻击场景描述、影响范围。

要求：
- 只分析安全问题，不要重写代码
- 重点关注：输入处理、文件系统操作、网络传输三个方面
- 如果某个风险在代码中已有部分防护但不完整，也要指出

[粘贴了 Client.py 完整代码]
```

**AI 响应关键片段（与安全约束相关）：**

> "发现以下安全风险：
> 1. **路径穿越（高危）**：`validate_filename()` 仅检查 `..` 字符，但未使用 `os.path.realpath()` 规范化路径，攻击者可通过 URL 编码 `%2e%2e` 或 Windows 风格 `..\\` 绕过校验...
> 2. **明文传输（中危）**：Socket 连接未加密，建议至少添加 HMAC 消息认证...
> 3. **写入内容未限制（中危）**：`receive_file()` 无文件大小上限，可导致磁盘耗尽..."

**偏差记录：** AI 同时建议"添加 TLS 支持"，但考虑到本项目为本地实验环境，已在约束文档中明确不引入新依赖，对此建议予以忽略，记录于 fix-report.md。

---

## Prompt 记录 #2 — 安全约束表达

**使用场景：** 将约束文档粘贴给 AI 后请求生成修复代码

**完整 Prompt：**

```
【项目约束文档——请在开始前完整阅读】
[粘贴了 constraint-doc.md 全文]

现在请基于以上约束，对下面的 Client.py 进行安全整改，
重点修复：
1. R-01 路径穿越：使用 os.path.realpath() + OUTPUT_DIR 白名单
2. R-03 接收内容未校验：添加 MAX_FILE_SIZE 100MB 限制 + 扩展名白名单
3. R-04 异常处理：将裸 except Exception 改为具体异常类型

请在每处修改旁添加注释说明修复的风险编号。
R-02（明文传输）本次暂不修复，仅添加警告注释。
禁止引入任何第三方库。

[粘贴了 Client.py 代码]
```

**AI 响应关键片段（安全约束落实情况）：**

> "已按照约束文档要求完成修复：
> - **R-01 修复**：在 `validate_filename()` 中新增 `os.path.realpath()` 规范化路径，并与 `OUTPUT_DIR` 对比，确保路径不超出允许目录
> - **R-03 修复**：新增 `ALLOWED_EXTENSIONS` 白名单集合 `{'.txt', '.pdf', '.py', '.json', '.md', '.csv'}`，`receive_file()` 开始前校验扩展名；新增 `MAX_FILE_SIZE = 100 * 1024 * 1024`，接收循环累计字节超限则中断并删除已写文件
> - **R-04 修复**：将 3 处裸 `except Exception` 改为 `except (socket.timeout, OSError) as e:`，安全相关异常（路径校验失败）改为 `raise`
> - 已在 R-02 相关的 `connect_server()` 处添加警告注释：'WARNING: 当前为明文传输，生产环境应使用 SSL/TLS 加密'"

**符合约束情况：** ✅ 全部约束得到落实，未引入第三方库，原有业务逻辑未被破坏。

---

## Prompt 记录 #3 — 审查验证

**使用场景：** 请 AI 对自己生成的代码进行二次审查

**完整 Prompt：**

```
请对以下修复后的 Client.py 进行安全审查：

检查项：
1. R-01 路径穿越防护是否完整？是否存在绕过可能？
2. R-03 文件大小限制是否在接收循环中正确累加？
3. 异常处理是否覆盖了所有关键路径？
4. 是否存在新引入的安全问题？

[粘贴修复后的代码]
```

**AI 审查发现（关键内容）：**

> "发现一个遗漏：`receive_file()` 在接收超限后删除临时文件时，未检查 `os.remove()` 的异常，若删除失败会留下不完整的大文件。建议在删除操作外包裹 `try/except OSError`。"

**处置：** 已根据审查意见补充 `os.remove()` 的异常处理，详见 fix-report.md。

---

*所有 AI 交互均发生在 claude.ai 网页版，对话截图已存档于本地作业提交材料中。*



---
# Prompt 记录

> **模块：** `成员代码/renyanbin/amia_defense_test.py` 安全整改  
> **记录人：** 谢智卓  

---

## Prompt 记录 #1 — 风险识别辅助

**使用场景：** 动手改代码前，用 AI 辅助梳理安全风险

**完整 Prompt：**

```
你是一位信息安全工程师。请审查以下面向多模态大模型安全性的自动化测试与防御框架代码，
识别其中存在的安全风险，并按严重程度（高/中/低）排序，
每个风险给出：漏洞类型、攻击场景描述、影响范围。

要求：
- 只分析安全问题，不要重写代码
- 重点关注：输入处理、算法本身逻辑等方面
- 如果某个风险在代码中已有部分防护但不完整，也要指出

[粘贴了 amia_defense_test.py 完整代码]
```

**AI 响应关键片段（与安全约束相关）：**

> "发现以下安全风险：
> 1. **路径遍历漏洞（文件读取）**：
> - **漏洞类型**：路径遍历（Path Traversal）  
> - **攻击场景**：攻击者通过控制数据文件 `data/figstep_samples.jsonl` 中的 `"image"` 字段，构造类似 > `"../../etc/passwd"` 的路径。...
> 2. Python 模块搜索路径污染（代码执行）**  
> - **漏洞类型**：模块注入 / 搜索路径劫持  
> - **攻击场景**：代码执行 `sys.path.append("../../")`，将当前工作目录的上级目录加入 Python 模块搜索路径。...
> ...

详细完整风险内容见`risk-analysis.md`文件。

---

## Prompt 记录 #2 — 安全约束表达

**使用场景：** 将约束文档粘贴给 AI 后请求生成修复代码

**完整 Prompt：**

```
【项目约束文档——请在开始前完整阅读】
[粘贴了 constraint-doc.md 全文]

现在请基于以上约束，对下面的amia_defense_test.py进行安全整改，
重点修复高风险的。

请在每处修改旁添加注释说明修复的风险编号。
禁止引入任何第三方库。

[粘贴了 amia_defense_test.py 代码]
```

**AI 响应关键片段（安全约束落实情况）：**

> | 风险编号 | 修复措施 |
> |---------|---------|
> | **R-01** | 引入 `safe_join_and_validate()`，对图片路径和主数据文件路径均使用 `os.path.realpath()` 规范化，并强制限制在 `data/` 目录内；禁止仅检查 `..`。 |
> | **R-02** | 移除 `sys.path.append("../../")`，改为计算当前脚本的绝对路径，定位项目根目录并验证 `amia.py` 存在后才加入 `sys.path`。 |
> | **R-03** | 对 `prompt` 进行长度限制（`MAX_PROMPT_LENGTH=4096`），超长则截断并记录警告。 |
> | **R-04** | 增加图片文件大小限制（50MB）和像素尺寸限制（16MP），加载前检查。 |
> | **R-05** | 为图片加载、模型推理、JSON解析等关键步骤添加细粒度异常捕获（`FileNotFoundError`, `ValueError`, `OSError`, `Image.UnidentifiedImageError`, `torch.cuda.OutOfMemoryError` 等），并用 `logging` 记录错误。 |
> | **R-06** | 引入 `truncate_for_log()` 函数对输出、意图分析、查询等日志内容截断至200字符；使用 `logging` 模块记录安全事件，不打印完整敏感信息。删除所有调试开关。 |
> | **R-07** | 写入结果前检查输出目录是否为符号链接，使用 `with open()` 上下文；限制单条日志记录大小（10KB）。 |
> | **R-08** | 删除注释掉的 `debug_idx` 及相关条件判断。 |

**符合约束情况：** ✅ 全部约束得到落实，未引入第三方库，原有业务逻辑未被破坏。

---

## Prompt 记录 #3 — 审查验证

**使用场景：** 请 AI 对自己生成的代码进行二次审查

**完整 Prompt：**

```
请对以下修复后的amia_defense_test.py进行安全审查：

检查项：
1. R-01 路径路径遍历漏洞是否修复？是否存在泄露文件可能？
2. R-02 是否解决模块搜索路径污染问题？
3. 异常处理是否覆盖了所有关键路径？
4. 是否存在新引入的安全问题？

[粘贴修复后的代码]
```

**AI 审查发现（关键内容）：**

> ...
> "**审查结论：** 整改后代码满足项目约束文档中 R‑01 至 R‑08 的所有要求，核心安全风险已消除，代码可合并入主分支。"


---
# Prompt 记录

> **模块：** `成员代码/fengyongjia/watermarkLSB.py` 安全整改  
> **记录人：** fengyongjia（冯永嘉）  
> **整改日期：** 2026-06-27

---

## Prompt 记录 #1 — 风险识别辅助

**使用场景：** 动手改代码前，用 AI 辅助梳理安全风险

**完整 Prompt：**

```
你是一位信息安全工程师。请审查以下改进的LSB图像隐写系统代码，
识别其中存在的安全风险，并按严重程度（高/中/低）排序，
每个风险给出：漏洞类型、攻击场景描述、影响范围。

要求：
- 只分析安全问题，不要重写代码
- 重点关注：输入处理、文件系统操作、路径安全、随机性安全四个方面
- 如果某个风险在代码中已有部分防护但不完整，也要指出

[粘贴了 watermarkLSB.py 完整代码]
```

**AI 响应关键片段（与安全约束相关）：**

> "发现以下安全风险：
> 1. **路径硬编码与路径穿越（中危）**：`Image.open('buptgray.bmp')` 和 `save()` 使用硬编码相对路径，若暴露给外部调用存在路径遍历风险...
> 2. **输入消息未校验（中危）**：`secret_msg` 无长度限制和字符集校验，可导致异常或日志注入...
> 3. **文件格式与大小未校验（中危）**：未限制图片像素/文件大小，存在内存耗尽风险...
> 4. **异常处理不完整（低危）**：最外层使用裸 `except Exception`...
> 5. **固定随机种子（低危）**：`np.random.seed(2021)` 硬编码，失去密钥控制意义...
> 6. **输出文件覆盖（低危）**：硬编码输出路径，多次运行直接覆盖..."

---

## Prompt 记录 #2 — 安全约束表达

**使用场景：** 将约束文档粘贴给 AI 后请求生成修复代码

**完整 Prompt：**

```
【项目约束文档——请在开始前完整阅读】
[粘贴了 constraint-doc.md 中 watermarkLSB 部分全文]

现在请基于以上约束，对下面的 watermarkLSB.py 进行安全整改，
重点修复：
1. R-01 路径硬编码与路径穿越：使用 os.path.realpath() + 安全目录校验
2. R-02 输入消息未校验：添加长度限制、非空检查、字符集校验
3. R-03 文件格式/大小/像素未校验：添加扩展名白名单、文件大小上限、像素上限
4. R-04 异常处理：将裸 except Exception 改为具体异常类型
5. R-05 固定随机种子：改为可配置种子，支持环境变量和基于时间的非确定性种子
6. R-06 输出文件覆盖：添加存在检查和时间戳重命名

请在每处修改旁添加注释说明修复的风险编号。
禁止引入任何第三方库。
核心LSB嵌入/提取算法保持不变。

[粘贴了 watermarkLSB.py 代码]
```

**AI 响应关键片段（安全约束落实情况）：**

> "已按照约束文档要求完成修复：
> - **R-01 修复**：新增 `safe_validate_path()` 函数，使用 `os.path.realpath()` 规范化路径并与安全目录边界比较；所有文件 I/O 操作前均调用此函数
> - **R-02 修复**：新增 `validate_message()` 函数，含三重校验：非空、长度上限 1024 字符、字符集白名单（ASCII 可打印 + 常用中文）
> - **R-03 修复**：新增 `ALLOWED_EXTENSIONS` 白名单、`MAX_IMAGE_FILE_SIZE = 50MB`、`MAX_IMAGE_PIXELS = 16MP`，在图像加载前/后分别校验
> - **R-04 修复**：将 5+ 处裸异常改为具体类型（`FileNotFoundError`、`PIL.UnidentifiedImageError`、`OSError`、`ValueError` 等），安全事件使用 `logging` 模块记录
> - **R-05 修复**：新增 `get_seed()` 函数，优先级为 环境变量 LSB_SEED → 基于时间+进程ID的非确定性种子；移除硬编码 `2021`
> - **R-06 修复**：新增 `safe_output_path()` 函数，写入前检查文件存在并自动添加时间戳后缀"

**符合约束情况：** ✅ 全部约束得到落实，未引入第三方库，原有 LSB 算法逻辑未被破坏。

---

## Prompt 记录 #3 — 审查验证

**使用场景：** 请 AI 对自己生成的代码进行二次审查

**完整 Prompt：**

```
请对以下修复后的 watermarkLSB_fixed.py 进行安全审查：

检查项：
1. R-01 路径穿越防护是否完整？`safe_validate_path()` 能否被绕过？
2. R-02 消息校验是否全面？空消息、超长消息、特殊字符是否都被拦截？
3. R-03 资源限制是否实际生效？文件大小、像素尺寸检查位置是否正确？
4. R-04 异常处理是否覆盖了所有关键路径？
5. R-05 种子获取函数在生产环境是否安全？
6. R-06 输出文件覆盖保护是否正确实现？
7. 是否存在新引入的安全问题？

[粘贴修复后的代码]
```

**AI 审查发现（关键内容）：**

> "**审查结论：** 整改后代码满足项目约束文档中 R‑01 至 R‑06 的所有要求，核心安全风险已消除。边缘情况（如符号链接路径、并发写入）在单机实验场景下可接受，代码可合并入主分支。"

**处置：** 根据审查结果，修复后代码通过安全审查，详见 fix-report.md。

---

# Prompt 完整记录

> **模块：** `成员代码/weichunru/DCT.py` — DCT 数字水印安全整改
> **记录时间：** 2026-06-27
> **记录人：** 位春汝（weichunru）

---

## Prompt 记录 #1 — 风险识别辅助

使用场景：动手改代码前，用 AI 辅助梳理安全风险。

**完整 Prompt：**

```
你是一名资深Python安全开发工程师。请审查以下 DCT 数字水印代码，
识别其中存在的安全风险，并按严重程度（高/中/低）排序，
每个风险给出：漏洞类型、攻击场景描述、影响范围。
要求：
- 只分析安全问题，不要重写代码
- 重点关注：身份认证、文件系统操作、异常处理三个方面
- 如果某个风险在代码中已有部分防护但不完整，也要指出

[粘贴了 DCT.py 完整代码]
```

**AI 响应关键片段（安全相关）：**

AI 识别出以下安全风险：

1. 缺少身份认证机制（高危）：原代码直接执行水印嵌入操作，未对调用者进行任何身份验证。
2. 文件路径硬编码（中危）：多处使用硬编码相对路径，未经 os.path.realpath() 规范化。
3. 输入文件校验不足（中危）：仅检查文件是否存在，未校验文件大小和格式。
4. 异常处理不完整（低危）：cv2.dct()、cv2.imwrite() 等操作未做细粒度异常捕获。

**处置决策：**

四项风险全部纳入修复范围。

---

## Prompt 记录 #2 — 安全约束表达与代码生成（R-01~R-04）

使用场景：将完整需求和约束提交给 AI 后，请求生成修复代码和全部文档。

**完整 Prompt：**

```
你是一名资深Python安全开发工程师。

我正在完成《信息安全管理》课程作业。
仓库项目：CyberSec-Practice-2026。
我的分工：身份认证（Authentication）
负责模块：成员代码/weichunru/DCT.py

任务：为DCT数字水印功能增加API Key身份认证接口。

要求：
1. 调用水印添加前必须验证API Key。
2. 新增verify_api_key()函数。
3. API Key错误立即拒绝执行。
4. API Key为空拒绝执行。
5. 不影响原有DCT算法。
6. 不引入第三方库。

```

**AI 响应关键片段（安全约束落实情况）：**

AI 确认已按照要求完成全部工作：

- R-01 修复：新增 `verify_api_key()` 函数（含 None/空/纯空白/类型检查四层防护）；定义 `VALID_API_TOKENS` 有效 Token 集合；在水印嵌入前添加验证网关；从环境变量 `DCT_API_KEY` 读取密钥；区分"未提供"和"无效"两种错误提示。
- R-02 修复：新增 `safe_resolve_path()` 函数，使用 `os.path.realpath()` + `os.path.basename()` 双保险剥离目录部分，所有文件路径均限制在脚本所在目录内。
- R-03 修复：新增 `validate_image_file()` 函数，校验扩展名白名单（仅 .bmp）和文件大小上限（50MB），在图像加载前执行。
- R-04 修复：主流程使用具体异常类型（FileNotFoundError、ValueError、OSError、AssertionError、cv2.error）替换裸异常，区分安全事件并给出明确错误提示。
- DCT 算法保护：所有原有变换逻辑保持完全不变。
- 无第三方库引入：仅使用 os + numpy + cv2 + matplotlib。
- 文档生成：按要求在 1/ 目录生成全部 7 份文档 + 汇总文档。

---

## Prompt 记录 #3 — AI 二次审查

使用场景：请 AI 对自己生成的代码进行安全审查。

**完整 Prompt：**

```
请对以下修复后的 DCT.py 进行安全审查：
检查项：
1. verify_api_key() 是否正确处理了 None、空字符串、纯空白、无效值四种情况？
2. API Key 验证网关是否在水印嵌入逻辑之前执行？
3. 是否存在绕过 API Key 验证的可能？
4. 是否存在新引入的安全问题？
5. 原有 DCT 算法是否保持完整？

[粘贴了修复后的完整代码]
```

**AI 审查发现：**

全部 5 项检查通过，未发现新安全问题。确认 verify_api_key() 四层防护完整、验证网关位置正确、DCT 算法零修改。



---

